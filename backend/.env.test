# Test environment variables
# ⚠️ SAFE TO COMMIT - These are test-only values, not production secrets
# Used by e2e tests in local dev
# CI overrides DATABASE_URL in the workflow with postgres:postgres credentials

DATABASE_URL=postgresql://rawsabsaid@localhost:5432/keystone_test
JWT_SECRET=test-jwt-secret-min-32-chars-long-for-testing-purposes-only
AWS_REGION=us-east-1
S3_BUCKET=test-bucket
PORT=3001

# Optional AWS credentials (not needed for tests, S3 is mocked)
# AWS_ACCESS_KEY_ID=test
# AWS_SECRET_ACCESS_KEY=test
